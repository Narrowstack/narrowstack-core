name: narrowstack_semantics_template
version: 1.0.0
config-version: 2
profile: narrowstack
model-paths: ["models"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  narrowstack_semantics_template:
    staging:
      +schema: marts_staging
    marts:
      +schema: marts_marts
