#!/usr/bin/env python3
"""ACL smoke test — verify each principal sees only permitted metrics."""
import sys


def main() -> int:
    print("acl smoke_test: stub (W1) — principal fixtures pending")
    return 0


if __name__ == "__main__":
    sys.exit(main())
