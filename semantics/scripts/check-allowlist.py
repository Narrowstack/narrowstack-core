#!/usr/bin/env python3
"""Default-deny allowlist check — fail with model name on violation."""
import sys


def main() -> int:
    print("check-allowlist: stub (W1) — default-deny enforcement pending")
    return 0


if __name__ == "__main__":
    sys.exit(main())
