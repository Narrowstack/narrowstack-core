# INVALID: secrets_refs values must be op:// references, never literal secret values.
tenant_id: bad-secret
warehouse:
  mode: external
  provider: postgres
  external_dsn_ref: op://Narrowstack/bad/WAREHOUSE_DSN
semantics_ref: v0.1.0
enabled_pipelines:
  - example_rest
allowlist_profile: default
principals:
  - id: admin
    tier: leadership
    metrics: ["*"]
provider_mode: M1
telemetry_grants: []
secrets_refs:
  agent_pg_password: hunter2-not-a-reference
