# INVALID: local mode requires service and volume properties.
tenant_id: bad-local-incomplete
vm_size_class: standard-4x16
warehouse:
  mode: local
  provider: postgres
semantics_ref: v0.1.0
enabled_pipelines:
  - example_rest
allowlist_profile: default
principals:
  - id: admin
    tier: leadership
    metrics: ["*"]
provider_mode: M0
telemetry_grants: []
backup:
  target_ref: op://Narrowstack/bad/BACKUP_TARGET
  retention_days: 30
